{
  "name": "pppoe-ip-checker",
  "module": "src/main.ts",
  "scripts": {
    "tsc": "tsc",
    "dev": "bun run --hot src/main.ts",
    "build": "bun build --minify --outfile dist/main.js --target bun src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mysql2": "^3.13.0",
    "nats": "^2.28.2",
    "pino": "^9.6.0"
  }
}
